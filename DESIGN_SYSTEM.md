# 디자인 시스템 포맷 가이드

이 문서는 설문 앱의 디자인 시스템을 다른 프로젝트에서 재사용하기 위한 가이드입니다.

## 📦 포함된 파일

### 1. 디자인 토큰 (Design Tokens)
- `client/src/styles/theme.css` - CSS 변수로 정의된 모든 디자인 토큰
- `client/src/index.css` - 전역 스타일 및 브랜딩 색상 설정

### 2. Tailwind 설정
- `client/tailwind.config.js` - Tailwind CSS 설정 및 커스텀 색상/간격/폰트

### 3. UI 컴포넌트
- `client/src/components/ui/` - 재사용 가능한 UI 컴포넌트들

---

## 🎨 디자인 토큰

### 색상 시스템

```css
/* Primary 색상 (브랜딩 컬러) */
--primary: #26C6DA;           /* 청록색 (기본값) */
--primary-hover: #00ACC1;     /* 호버 상태 */

/* Secondary 색상 */
--secondary: #F59E0B;         /* 주황색 */
--success: #10B981;           /* 초록색 */
--error: #EF4444;             /* 빨간색 */

/* 텍스트 색상 */
--text-main: #111827;         /* 메인 텍스트 */
--text-sub: #6B7280;          /* 서브 텍스트 */

/* 배경 색상 */
--bg: #F9FAFB;                /* 배경 */
--white: #FFFFFF;             /* 흰색 */
--border: #E5E7EB;            /* 테두리 */
```

### 간격 시스템

```css
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.25rem;   /* 20px */
--space-6: 1.5rem;    /* 24px */
--space-8: 2rem;      /* 32px */
--space-10: 2.5rem;   /* 40px */
--space-12: 3rem;     /* 48px */
```

### 둥근 모서리

```css
--radius-sm: 4px;
--radius-md: 12px;
--radius-lg: 16px;
--radius-xl: 20px;
--radius-full: 9999px;
```

### 폰트 시스템

```css
/* 폰트 패밀리 */
--font-display: 'Pretendard', -apple-system, BlinkMacSystemFont, ...;
--font-body: 'Pretendard', -apple-system, BlinkMacSystemFont, ...;

/* 폰트 크기 */
--font-size-xs: 0.75rem;    /* 12px */
--font-size-sm: 0.875rem;   /* 14px */
--font-size-base: 1rem;     /* 16px */
--font-size-lg: 1.125rem;   /* 18px */
--font-size-xl: 1.25rem;    /* 20px */
--font-size-2xl: 1.5rem;    /* 24px */
--font-size-3xl: 1.875rem;  /* 30px */
--font-size-4xl: 2.25rem;  /* 36px */

/* 폰트 두께 */
--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;
```

### 그림자 시스템

```css
--shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
--shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
```

---

## 🧩 UI 컴포넌트 구조

### 공통 컴포넌트

1. **Button (버튼)**
   - Primary 버튼: 그라데이션 배경, 흰색 텍스트
   - Secondary 버튼: 테두리만, 회색 텍스트
   - 둥근 모서리: `rounded-lg` (기본)

2. **Input Field (입력 필드)**
   - 배경: 흰색 또는 반투명 흰색
   - 테두리: 2px, Primary 색상
   - 둥근 모서리: `rounded-lg`
   - 폰트: 15px, 400 weight

3. **Card (카드)**
   - 배경: 흰색
   - 그림자: `shadow-md` 또는 `shadow-lg`
   - 둥근 모서리: `rounded-xl`
   - 패딩: `p-4` ~ `p-6`

4. **Progress Bar (진행률 바)**
   - 투명도: 90%
   - 높이: 4px
   - 그라데이션 배경

---

## 📐 레이아웃 패턴

### 관리자 페이지 레이아웃
```
┌─────────────────────────────────────────┐
│  Header (고정)                          │
│  - 탭 네비게이션                        │
│  - 저장 버튼                            │
├──────────────┬──────────────────────────┤
│              │                          │
│  좌측 패널    │  메인 콘텐츠 영역        │
│  (설정/목록)  │  (폭: flex-1)            │
│              │                          │
│              │  ┌────────────────────┐  │
│              │  │  모바일 미리보기   │  │
│              │  │  (고정 너비)       │  │
│              │  └────────────────────┘  │
└──────────────┴──────────────────────────┘
```

### 참가자 페이지 레이아웃 (모바일 퍼스트)
```
┌─────────────────────┐
│  진행률 바 (상단 고정) │
├─────────────────────┤
│                     │
│  질문 카드           │
│  (중앙 정렬)         │
│                     │
│  입력 필드           │
│  (80% 너비)          │
│                     │
├─────────────────────┤
│  네비게이션 버튼     │
│  (하단 고정)         │
└─────────────────────┘
```

---

## 🎯 사용 가이드

### 1. 프로젝트에 적용하기

#### Step 1: 파일 복사
```bash
# 디자인 토큰 파일 복사
cp client/src/styles/theme.css [새프로젝트]/src/styles/theme.css
cp client/src/index.css [새프로젝트]/src/index.css

# Tailwind 설정 복사
cp client/tailwind.config.js [새프로젝트]/tailwind.config.js
```

#### Step 2: CSS Import
```css
/* index.css 또는 main.css */
@import "tailwindcss";
@import "./styles/theme.css";
```

#### Step 3: Tailwind 설정 확인
`tailwind.config.js`의 `content` 경로를 새 프로젝트에 맞게 수정

### 2. 색상 커스터마이징

```css
/* theme.css에서 기본 색상 변경 */
:root {
  --primary: #YOUR_COLOR;        /* 브랜드 Primary 색상 */
  --primary-hover: #YOUR_HOVER;  /* 호버 색상 */
  --secondary: #YOUR_SECONDARY;  /* Secondary 색상 */
}
```

### 3. 컴포넌트 사용 예시

```jsx
// 버튼
<button className="px-6 py-3 bg-primary text-white rounded-lg font-semibold hover:opacity-90">
  버튼
</button>

// 입력 필드
<input 
  className="w-full px-5 py-3.5 rounded-lg border-2 border-primary bg-white"
  style={{ fontSize: '15px' }}
/>

// 카드
<div className="bg-white rounded-xl shadow-md p-6">
  카드 내용
</div>
```

---

## 📋 체크리스트

다른 프로젝트에 적용할 때 확인할 사항:

- [ ] `theme.css` 파일이 프로젝트에 포함되어 있는가?
- [ ] `index.css`에서 `theme.css`를 import 했는가?
- [ ] `tailwind.config.js`의 `content` 경로가 올바른가?
- [ ] Pretendard 폰트가 로드되어 있는가? (또는 다른 폰트로 변경)
- [ ] Primary 색상을 브랜드에 맞게 변경했는가?
- [ ] 필요한 UI 컴포넌트를 복사했는가?

---

## 🔗 관련 파일 위치

- **디자인 토큰**: `client/src/styles/theme.css`
- **전역 스타일**: `client/src/index.css`
- **Tailwind 설정**: `client/tailwind.config.js`
- **UI 컴포넌트**: `client/src/components/ui/`

---

## 💡 팁

1. **색상 변경**: `theme.css`의 CSS 변수만 수정하면 전체 색상이 일괄 변경됩니다.
2. **반응형**: Tailwind의 기본 반응형 클래스(`sm:`, `md:`, `lg:` 등)를 사용하세요.
3. **접근성**: 터치 타깃 최소 크기(`--touch-target-min: 44px`)를 준수하세요.
4. **일관성**: 디자인 토큰을 사용하여 일관된 디자인을 유지하세요.

